[workspace]
members = ["cargo:."]

# Config for 'dist'
[dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.32.0"
# CI backends to support
ci = "github"
# The installers to generate for each app
installers = ["shell", "npm", "homebrew"]
# Target platforms to build apps for (Rust target-triple syntax)
targets = ["aarch64-apple-darwin", "aarch64-unknown-linux-gnu", "x86_64-apple-darwin", "x86_64-unknown-linux-gnu", "x86_64-pc-windows-msvc"]
# Path that installers should place binaries in
install-path = "CARGO_HOME"
# Where to host releases
hosting = "github"
# Whether to install an updater program
install-updater = false
# Publish the generated Homebrew formula (the CLI binary) to this tap repo.
tap = "Entelligentsia/homebrew-tap"
publish-jobs = ["homebrew", "npm"]
# The `asciicut` binary embeds the SolidJS SPA via rust-embed at compile time, so
# the web bundle must be built BEFORE `dist build` runs cargo. These steps are
# injected into each build job — see .github/workflows/build-setup.yml.
github-build-setup = "build-setup.yml"
